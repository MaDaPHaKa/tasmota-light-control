import { ChangeDetectionStrategy, Component, input, output } from '@angular/core'; import { MatButtonModule } from '@angular/material/button'; import { ControlOperationResult } from '@model/control.model';
@Component({selector:'app-operation-results',standalone:true,imports:[MatButtonModule],templateUrl:'./operation-results.component.html',styleUrl:'./operation-results.component.scss',changeDetection:ChangeDetectionStrategy.OnPush}) export class OperationResultsComponent {readonly result=input<ControlOperationResult|null>(null);readonly retryFailed=output<string[]>();protected state(){const r=this.result();if(!r)return 'idle';if(r.summary.failed===0)return 'success';return r.summary.succeeded?'partial failure':'failure';}}
